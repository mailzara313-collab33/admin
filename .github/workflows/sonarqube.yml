# SonarQube ayrı sunucu gerektirdiğinden devre dışı bırakıldı.
# Yerine sonarcloud.yml kullanılıyor (ucretsiz, bulut tabanlı).
name: SonarQube analysis (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "SonarQube disabled. Use SonarCloud (sonarcloud.yml) instead."
