# Fortify enterprise lisansı gerektirdiğinden devre dışı bırakıldı.
# Yerine security-pipeline.yml içindeki Semgrep SAST kullanılıyor.
# Fortify lisansı edinilirse bu dosya yeniden aktive edilebilir.
name: Fortify AST Scan (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Fortify disabled. Use Semgrep in security-pipeline.yml instead."
